# Strategy candidate slate — no live activation

Generated: 2026-04-27
Scope: candidate generation/ranking only. Uses repo runtime artifacts and first-principles strategy analysis. No live trading or activation recommended.

## Grounding from repo/runtime

- Repo stance: paper only; live trading blocked; do not promote without settled paper evidence.
- Current active families: `toxicity_mm`, `time_decay`; candidates: `mean_reversion_5min`, `opening_range`, `spot_momentum`.
- Current status gate: RED; new_order_pause=true due to run_lineage_fragmentation and unreviewed circuit breaker.
- Current-run evidence: 0 fills, 0 settled trades; `time_decay` and `toxicity_mm` have seen 1,784 markets each but submitted 0 quotes/fills in latest metrics.
- Historical fill markout artifact: 56,914 fills analyzed; 46,530 settled; 58.9% adverse final markouts; avg final markout about -1.26c/share. Adverse selection is the dominant design constraint.
- Reward research: current 5m crypto target markets sampled had reward configs but effectively $0/day rewards; broader non-5m rewards may be viable only as a separate capped/model-backed experiment.

## Ranked slate

| Rank | Strategy | Class | Thesis | Edge source | Data required | Entry | Exit | Order placement | Risk controls | Failure modes | Capacity | Complexity | Paper test | Evidence before activation | Recommendation |
|---:|---|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 1 | `spot_fair_value_terminal` | Trade candidate | For 5m/15m Up/Down, compute terminal win probability from underlying spot vs slot open plus remaining-time volatility; buy only when market price is below model fair by enough to cover adverse selection. | Independent fair-value model rather than book microstructure alone; strongest antidote to historical toxic fills. | Slot open, current spot, realized vol by asset/time-of-day, tte, market price/book, settlement truth. | Buy Up/Down only when `fair - ask >= edge_threshold` and tte/liquidity gates pass. Prefer mid-slot through late-mid, not final seconds unless edge large. | Settle at expiry; optional de-risk if market reprices to fair or edge collapses; no averaging down. | Resting limit inside ask only if edge survives; otherwise IOC-style paper fill at best ask in backtest. TTL 3-5s. | Max $2-$6 paper notional initially; one position per slot/asset; no fresh entries under 30-60s unless model edge > 4-6c; pause on spot feed stale/vol spike. | Vol model miscalibration; oracle/settlement mismatch; market already embeds same fair; latency; correlated asset exposure. | Medium in 5m majors; constrained by liquidity and edge frequency. | Medium. | Shadow fair values every loop; then paper trade with tiny size after gate green. Report Brier/calibration and markout. | >=200 scored slots, calibration error acceptable, >=50 paper trades, positive EV after conservative slippage, adverse final markout <55%, no unresolved settlement issues. | Top implementation candidate. Build as scanner first, then paper-only strategy. |
| 2 | `spot_confirmed_opening_range` | Trade candidate | Initial move after slot open can persist, but only if confirmed by spot move from slot-open and book repricing lag. | Momentum/underreaction in short-duration crypto binaries after a confirmed opening range break. | Per-slot first N ticks, spot open/current, book mid, tte, volume/depth. | After range frozen, enter only if spot has also moved beyond threshold and predicted terminal fair agrees. Avoid pure market-price breakouts. | Settle or exit if spot retraces into range/fair edge disappears. | Resting limit at fair-minus-buffer; TTL <=5-10s; one fire per slot. | Require min depth, max spread, no stale spot, no duplicate slot fire; cap notional; no entries after late tte. | False breakouts/chop; current code size fixed 10 shares and may overtrade; volume threshold may prevent signals. | Medium. | Low/Medium because existing `opening_range` exists; needs fair-value confirmation and sizing fixes. | Run alongside current candidate, compare raw vs confirmed. | >=50 settled confirmed trades, p/trade > +$0.03-$0.05, win rate not below model-implied threshold, clear improvement over unconfirmed opening_range. | Top implementation candidate after fair-value module. |
| 3 | `new_market_stale_quote_scanner` | Scanner -> possible trade | Newly listed/rolled interval slots may inherit stale/manual quotes before fair value catches up. Detect quotes inconsistent with current spot-implied fair. | Stale quote dislocation at slot creation; can be captured only when fair model is strong and stale side is resting. | Market discovery timestamps, first book snapshots, slot open, current spot, fair model, quote age if available. | Scanner flags when best ask/bid is far from fair within first 10-45s of slot and liquidity is fillable. | Immediate close/settle; cancel if book updates or fair edge vanishes. | Initially scanner-only; if traded, aggressive best ask/bid with tight size because stale quotes disappear fast. | Very small size; require fresh local spot; reject wide/missing books; one trade per slot; fast cooldown. | Apparent staleness can be justified by hidden info; discovery latency; bad slot-open price; quote disappears. | Low/Medium; edge rare but high quality. | Medium. | Log all open-slot dislocations and next-snapshot/settlement markout. | >=100 dislocations, fill-sim edge persists after latency/slippage, positive final simulated PnL, low false positives. | Top implementation candidate as scanner first. |
| 4 | `spot_skewed_market_making` | Trade candidate | Make markets around spot-implied fair, skewing quotes by inventory and directional spot pressure instead of raw midpoint. | Spread capture only where model says both sides are non-toxic; skew avoids adverse side. | Fair model, order book, spot velocity, inventory, fill markouts. | Post bid/ask around fair when spread wide enough and both quotes have positive expected value; one-sided quote allowed only with strong edge. | Cancel/reprice on fair move >=0.25-0.5c, fill, spread blowout, tte gate. | Passive post-only paper quotes; TTL 3-10s; quote farther from fair than adverse buffer. | Inventory limits by asset/slot; adverse fill burst pause; quote only in early/mid tte. | Historical data shows passive fills are adverse; if fair model weak, becomes toxicity_mm repeat. | Medium/high if edge valid; otherwise low. | Medium/High. | A/B vs `toxicity_mm`; track 30/60/120/final markouts. | >=100 fills with avg final markout >=0 after spread, adverse share <55%, realized PnL positive after settlement. | Promising, but only after fair-value model exists. |
| 5 | `adverse_selection_pause_model_v2` | Guard/scanner | A predictive pause model can remove toxic periods from any maker/directional family. | Loss avoidance from historical adverse selection. | Book imbalance/depth ratio/spread changes, spot velocity, tte, prior fills, markout labels. | Not an entry strategy; emits allow/pause and side-specific veto. | Resume after cooldown and normalized microstructure. | Applies to all quote/trade decisions. | Conservative default; log opportunity cost and missed winners. | Over-pausing causes no fills; labels contaminated by bad old strategies; regime shift. | High as a guard. | Medium. | Train/evaluate offline, then shadow veto live paper decisions. | Out-of-sample reduces adverse markout materially without killing all positive trades; clear confusion matrix by family. | Top scanner/guard candidate; implement before expanding makers. |
| 6 | `spot_fair_mean_reversion` | Trade candidate | If market overshoots spot-implied fair due to panic/flow, fade back toward fair. Different from EMA mean reversion: anchor to model fair. | Market-price overreaction relative to independent fair. | Fair model, market mid, book imbalance, spot move/vol, tte. | Buy underpriced side or sell/avoid overpriced side only when deviation from fair exceeds threshold and spot momentum is decelerating. | Exit when price reverts to fair or settle; stop if fair moves against entry. | Resting limit, not chase; TTL 5-10s. | Avoid strong spot trend; require liquidity; cap per slot; no late entries without large edge. | Catches falling knives in trending windows; fair model lag; sell leg may be unavailable/complex. | Medium. | Medium. | Shadow deviations; paper only after fair model calibration. | >=50 trades, positive mean reversion markout at 30-120s and final, performance by asset stable. | Candidate, but ranked below momentum/stale scanner until model exists. |
| 7 | `complementary_dislocation_scanner` | Scanner | Binary complements should sum near 1. Books can briefly imply impossible/cheap packages. | Mechanical complement parity deviations. | Both outcome books/token ids, best bid/ask, fees/tick constraints, fill simulation. | Flag `ask(Up)+ask(Down)<1-buffer` buy package, or `bid(Up)+bid(Down)>1+buffer` sell package if inventory/execution allows. | Resolve guaranteed package at $1 or unwind when parity normalizes. | Scanner-only initially; paper simulate simultaneous legs with partial-fill modeling. | Require both legs executable; account for tick/fees/slippage; reject thin/deceptive size. | Partial fills; complement semantics/token mapping errors; package not actually riskless due to market rules. | Low in efficient current slots; high confidence when found. | Low/Medium scanner. | Log parity deviations with executable size and next-book persistence. | Multiple executable simulated packages with post-latency edge >1-2c and no mapping errors. | Top scanner-only. |
| 8 | `cross_market_complement_arbitrage` | Scanner/reject for auto | Same logical event across venues/related Polymarket markets may imply inconsistent complements. | Relative-value parity across duplicated/correlated markets. | Market taxonomy, condition ids, resolution rules, books, fees, settlement semantics. | Enter paired positions only if resolution rules identical and both sides executable. | Hold to settlement or unwind convergence. | Must be atomic or heavily partial-fill controlled. | Manual review for rule mismatch; tiny size; no live auto initially. | Resolution mismatch, correlated-not-identical markets, liquidity mirage, partial fills. | Low; true duplicates rare. | High due to matching/rules. | Scanner with human review queue. | Documented identical rules plus repeated executable arb after latency. | Scanner-only, not implementation priority. |
| 9 | `whale/orderflow follower-fader` | Scanner | Public Data API can reveal large wallet/orderflow behavior; whales may be informed or may overpush prices. | Follow informed flow or fade exhausted/uninformed flow. | Public trades/order events by wallet/market/time, price impact, subsequent markouts, wallet clustering. | Scanner emits follow/fade candidates after wallet-specific historical edge is proven. | Short holding or settlement depending on horizon. | No orders until wallet edge validated; if traded, small directional limits. | Anonymity/clustering uncertainty; avoid doxxing/private data; rate limits; overfitting. | Public flow may be delayed/noisy; whales can hedge elsewhere; survivorship bias. | Medium if many markets; for 5m may be latency constrained. | High. | Build wallet leaderboard by subsequent 30/120/final markout. | Statistically significant wallet cohorts after costs; stable out-of-sample. | Top scanner-only; not trade-active yet. |
| 10 | `reward_aware_maker` | Separate experiment | Non-5m reward markets may pay enough to offset adverse selection if fair-value model exists. 5m crypto rewards currently not viable. | Liquidity rewards plus spread capture. | Rewards API, market metadata, competition proxy, fair value model by category, fill/reward attribution. | Quote only markets with nonzero rewards, affordable min size, low competition, strong fair value. | Cancel on news/fair move/fill burst; hold inventory only within category risk caps. | Two-sided passive near fair; min incentive size; TTL 5-30s depending toxicity. | Separate bankroll; category caps; news gates; reward haircut 70-90%; adverse markout kill. | Toxic event risk; requires domain models; current bot risk size below reward min; reward competition changes. | Potentially high outside 5m, but not in current bot universe. | High. | Read-only rewards scanner first; paper reward attribution later. | Actual/user reward share validation plus positive PnL net conservative reward estimate. | Scanner/research-only for this bot; separate project if pursued. |
| 11 | `strict_time_decay_v2` | Trade candidate/reject current form | Late binary prices should converge to 0/1, but without independent terminal confidence this is mostly paying up near expiry. | Possible underpricing of likely winner in final 10-60s. | Spot terminal fair, oracle timing, late book, settlement. | Buy only if terminal fair > price + very large buffer and spot has confirmed winner; reject simple price>0.55 rule. | Settle; no time to exit. | Resting limit below fair; TTL <=3s; tiny size. | Edge threshold 6-10c; no entries under 10s; one fire; high confidence only. | Historical adverse selection worst around late windows; current time_decay emitted no orders/fills; simple thesis too weak. | Low/Medium. | Low to modify existing, but high risk. | Shadow strict filters; compare to current time_decay. | >=30 settled late trades with strong positive final markout; no current-run contradictions. | Do not activate current version; only revisit as terminal-fair gated v2. |
| 12 | `long_dated_holding/reward_carry_scanner` | Scanner | Long-dated reward/carry markets may offer attractive reward yield or mispriced slow fair values. | Reward carry and slower information diffusion. | Rewards API, market metadata, external fair-value sources, borrow/capital lock, news calendar. | Scanner identifies high reward-per-risk and stable fair-value markets; manual review. | Daily reward accrual or fair-value convergence; exit on news/risk change. | Passive reward-sized quotes only after separate approval. | Separate bankroll and risk model; category correlation caps; no live auto. | Not aligned with 5m bot; capital lock; event risk; model burden. | Potentially medium/high outside scope. | High. | Read-only scanner. | Verified actual reward capture and model-backed fair value. | Scanner-only; not a 5m bot implementation candidate. |

## Top 3 implementation candidates

1. `spot_fair_value_terminal`
   - Build the model/scanner first. It is prerequisite infrastructure for almost every safer directional or maker strategy.
   - Promotion path: shadow calibration -> paper directional entries -> maker/skew integration.

2. `spot_confirmed_opening_range`
   - Existing `opening_range` code is close, but should require spot/fair confirmation and dynamic notional.
   - It can produce interpretable one-fire-per-slot paper evidence quickly.

3. `new_market_stale_quote_scanner`
   - Best asymmetric edge candidate if fair model is available.
   - Start scanner-only; only paper trade after latency-adjusted stale quote evidence.

## Top 3 scanner-only / guard candidates

1. `adverse_selection_pause_model_v2`
   - Should become a shared guard before any maker expansion.

2. `complementary_dislocation_scanner`
   - Low-risk scanner, clear mechanical edge when executable, useful for alerts/research.

3. `whale/orderflow follower-fader`
   - Valuable public Data API research surface, but requires statistically validated cohorts before trading.

Honorable scanner-only: `reward_aware_maker` and `long_dated_holding/reward_carry_scanner` for a separate non-5m research track.

## Rejects / do-not-activate now

- Current `strict_time_decay` / `time_decay`: no current-run fills, simple price>0.55 late-entry thesis is not enough, and historical markouts are adverse. Rebuild only as terminal-fair-gated `strict_time_decay_v2`.
- Current `toxicity_mm` as a standalone baseline expansion: historical passive fills show adverse selection; it should not be scaled without fair-value skew and pause model.
- `reward_aware_maker` in current 5m crypto universe: reward rate sampled as effectively zero and current risk sizing below common reward min sizes.
- `cross_market_complement_arbitrage` auto-trading: rule matching/partial-fill complexity makes it scanner/manual-review only.
- Pure EMA `mean_reversion_5min`: replace/upgrade to `spot_fair_mean_reversion`; market EMA alone is too endogenous.

## Universal activation gates

No live activation. For any paper promotion:

- Gate state must be GREEN; resolve unreviewed circuit breaker and run fragmentation first.
- Maintain paper-only mode.
- Minimum evidence: at least 50 settled paper trades for directional candidates or 100 fills for maker candidates, unless scanner-only.
- Required metrics: realized PnL, final markout, 30/60/120s markouts, adverse share, fill rate, missed opportunity rate, per-asset breakdown, settlement audit.
- Strategy must beat a no-trade baseline and current family baseline after conservative slippage/latency assumptions.
- Hard kill if adverse final markout >60%, settlement mismatches appear, or PnL per trade < -$0.20 after 20+ settled trades.
